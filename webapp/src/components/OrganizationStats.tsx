import React from "react";
import { Paper } from "@mui/material";

const OrganizationStats: React.FC = () => {
  return (
    <Paper style={{ padding: 24, borderRadius: 20, background: "#fff", minHeight: 630, boxShadow: "0 2px 16px #bfcbe6", display: "flex", alignItems: "center", justifyContent: "center" }}>
      
    </Paper>
  );
};

export default OrganizationStats;
